# Upstart script for the sponsorship form backend.
#
# {{ ansible_managed }}

respawn

start on runlevel [2345]
stop on runlevel [06]

script
  exec start-stop-daemon --start --chuid vagrant:www-data \
    --exec /usr/bin/node -- {{ backendroot }}/index.js
end script
