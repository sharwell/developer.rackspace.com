---
# Copy this file to "secrets.yml" and fill in these values with the credentials
# for a Rackspace cloud account to test the node.js backend.

# If you're running in dev mode with Vagrant, run `vagrant provision` after you
# modify secrets.yml for changes to take effect.

rackspace_username:
rackspace_apikey:

# Mailgun credentials
mailgun_apikey:
mailgun_domain:

# Sponsorship form destinations
sponsorship_notification_email:
sponsorship_container:

# PagerDuty credentials
pagerduty_servicekey:

# MongoDB connection URL
mongodb_connection_url: