.. code-block:: javascript

  client.destroyContainer(container, function(err) {
    if (err) {
      // TODO handle as appropriate
    }
  });

.. code-block:: python

  container.delete()

.. code-block:: ruby

  directory.destroy
